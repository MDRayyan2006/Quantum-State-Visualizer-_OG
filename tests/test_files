#!/usr/bin/env python3
"""
Simple test script to verify backend dependencies work correctly
"""

def test_imports():
    """Test if all required packages can be imported"""
    try:
        print("Testing imports...")
        
        # Core packages
        import fastapi
        print(f"✅ FastAPI {fastapi.__version__}")
        
        import uvicorn
        print(f"✅ Uvicorn {uvicorn.__version__}")
        
        # Quantum packages
        import qiskit
        print(f"✅ Qiskit {qiskit.__version__}")
        
        from qiskit_aer import Aer
        print("✅ Qiskit Aer")
        
        import qutip
        print(f"✅ QuTiP {qutip.__version__}")
        
        # Scientific packages
        import numpy
        print(f"✅ NumPy {numpy.__version__}")
        
        import scipy
        print(f"✅ SciPy {scipy.__version__}")
        
        # Visualization packages
        import plotly
        print(f"✅ Plotly {plotly.__version__}")
        
        import dash
        print(f"✅ Dash {dash.__version__}")
        
        # Data packages
        import pandas
        print(f"✅ Pandas {pandas.__version__}")
        
        import pydantic
        print(f"✅ Pydantic {pydantic.__version__}")
        
        print("\n🎉 All imports successful! Backend should work correctly.")
        return True
        
    except ImportError as e:
        print(f"❌ Import error: {e}")
        return False
    except Exception as e:
        print(f"❌ Unexpected error: {e}")
        return False

def test_basic_functionality():
    """Test basic quantum functionality"""
    try:
        print("\nTesting basic quantum functionality...")
        
        # Test Qiskit circuit creation
        from qiskit import QuantumCircuit
        circuit = QuantumCircuit(2, 2)
        circuit.h(0)
        circuit.cx(0, 1)
        circuit.measure([0, 1], [0, 1])
        print("✅ Qiskit circuit creation")
        
        # Test QuTiP basic operations
        import qutip as qt
        state = qt.basis(2, 0)
        print("✅ QuTiP basic operations")
        
        # Test NumPy operations
        import numpy as np
        arr = np.array([[1, 0], [0, 1]])
        print("✅ NumPy operations")
        
        print("🎉 Basic functionality tests passed!")
        return True
        
    except Exception as e:
        print(f"❌ Functionality test failed: {e}")
        return False

if __name__ == "__main__":
    print("🧪 Testing Quantum State Visualizer Backend\n")
    
    imports_ok = test_imports()
    if imports_ok:
        functionality_ok = test_basic_functionality()
        if functionality_ok:
            print("\n🚀 Backend is ready to run!")
        else:
            print("\n⚠️  Backend has functionality issues")
    else:
        print("\n❌ Backend has import issues")
